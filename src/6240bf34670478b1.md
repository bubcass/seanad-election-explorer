# Dáil elections | November 2024

```js
intro = md`<div class="simple-content normal"><p>The latest general election took place in November 2024 and [174 Members were returned to Dáil Éireann from 43 constituencies](https://www.oireachtas.ie/en/members/).</p>
<h2>Explore the election by selecting a constituency.</h2></div>`
```

```js
viewof selectConstituency = Inputs.select(constituencies, {
  label: "Constituency",
  width: 750
})
```

```js
constituencyMapExperimentClickablewithLinks = {
  const container = yield htl.html`<div style="height: 400px;">`;
  const map = L.map(container);

  // Styling for the GeoJSON layer
  function myStyle(feature) {
    return {
      fillColor: "green",
      weight: 2,
      opacity: 1,
      color: "grey",
      dashArray: 1,
      fillOpacity: 0.2
    };
  }

  // Function to handle each feature in the GeoJSON
  function onEachFeature(feature, layer) {
    if (feature.properties && feature.properties.ENG_NAME_VALUE) {
      layer
        .bindPopup(
          "This is the " +
            `<strong>${feature.properties.ENG_NAME_VALUE.slice(
              0,
              -4
            )}</strong>` +
            " constituency. " +
            `<br><br>` +
            `${generateTDsSentence(tds)}`
        )
        .openPopup();
    }
  }

  // Add GeoJSON layer to the map
  const geojsonLayer = L.geoJSON(filtered, {
    style: myStyle,
    onEachFeature: onEachFeature // Attach popup functionality
  });

  // Check if the GeoJSON has valid data and add it to the map
  if (geojsonLayer.getLayers().length > 0) {
    geojsonLayer.addTo(map);

    // Fit bounds to the single constituency
    map.fitBounds(geojsonLayer.getBounds(), { maxZoom: 19 });

    // Open the popup for the first layer explicitly
    const firstLayer = geojsonLayer.getLayers()[0];
    if (firstLayer) {
      firstLayer.openPopup();
    }
  } else {
    console.warn("No valid features found in the GeoJSON data.");
  }

  // Add a tile layer to the map
  L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
    attribution:
      "© <a href=https://www.openstreetmap.org/copyright>OpenStreetMap</a> contributors"
  }).addTo(map);
}
```

```js
raceIntro = md`<h3>The story of each election count</h3>
<div class="simple-content normal"><p>Take an <strong>interactive look</strong> at how the general election unfolded in November 2024 and the story of each count.</p></div>`
```

```js
chart = {
  replay;

  const svg = d3
    .create("svg")
    .attr("viewBox", [0, 0, width, height])
    .attr("width", width)
    .attr("height", height)
    .attr("style", "max-width: 100%; height: auto;");

  const updateBars = bars(svg);
  const updateAxis = axis(svg);
  const updateLabels = labels(svg);
  const updateTicker = ticker(svg);

  yield svg.node();

  for (const keyframe of keyframes) {
    const transition = svg.transition().duration(duration).ease(d3.easeLinear);

    // Extract the top bar’s value.
    x.domain([0, keyframe[1][0].value]);

    updateAxis(keyframe, transition);
    updateBars(keyframe, transition);
    updateLabels(keyframe, transition);
    updateTicker(keyframe, transition);

    invalidation.then(() => svg.interrupt());
    await transition.end();
  }
}
```

```js
viewof replay = html`<button>Replay`
```

```js
disclaimer = htl.html`<strong>Charts and tables on this page are illustrative and may not reflect the exact state of the poll.<br><br>
Find out more.`
```

```js
inRoom = html`<h3>How the election unfolded</h3>
<p>The election concluded after <strong>count No. ${
  resultCount[0].count
}</strong>, when <strong>${
  finalElected.length
}</strong> candidates were deemed elected on this panel from a field of <strong>${
  Array.from(names).length
} candidates</a></strong>.</p>
<div class="observablehq__row">
  <div class="observablehq__col">
    <h4>Candidates deemed elected:</h4>
    <ul class="observablehq__list">
      ${finalElectedList.map(
        (name) => html`<li>${name.replace(/,\s*$/, "")}</li>`
      )}
    </ul>
  </div>
  <div class="observablehq__col">
    <h4>Candidates excluded:</h4>
    <ul class="observablehq__list">
      ${[...finalEliminatedList].map(
        (name) => html`<li>${name.replace(/,\s*$/, "")}</li>`
      )}
    </ul>
  </div>
</div>`
```

```js
viewof countNo = {
  const input = Inputs.range([1, totalCount], {
    label: "After Count No.",
    step: 1,
    value: totalCount
  });

  const label = input.querySelector("label");
  const slider = input.querySelector('input[type="range"]');

  // Assign an ID to the label (if it doesn't already have one)
  label.id = "after-count-no-label";

  // Associate the slider with the label
  slider.setAttribute("aria-labelledby", label.id);

  return input;
}
```

```js
waffleVotes = Plot.plot({
  style: {
    fontFamily: "IBM Plex Sans",
    fontSize: 14,
    padding: "5px"
  },
  color: {
    legend: true,
    swatchWidth: 25,
    columns: 3,
    marginLeft: 10,
    label: "Count status",
    domain: ["Elected", "Continuing", "Excluded"],
    order: ["Elected", "Continuing", "Excluded"],
    range: ["#ff7f0e", "#1f77b4", "#dadbdc"]
  },
  axis: null,
  width: 1000,
  label: null,
  height: 300,
  marginTop: 20,
  marginBottom: 100,
  subtitle: htl.html`Candidates according to <strong>percentage of quota filled</strong>. The quota was <strong>${format(
    quota
  )}</strong> votes and the number of constituency seats is <strong>${
    seats[0]
  }</strong>.<br><br>`,
  marks: [
    Plot.axisFx({
      lineWidth: 1,
      //tickRotate: 15,
      anchor: "bottom",
      dy: 30,
      fontWeight: "bold"
      //tickRotate: 15
    }),
    Plot.waffleY(
      { length: 1 },
      {
        y: 100,
        fillOpacity: 0.4,
        rx: "100%"
      }
    ),
    Plot.waffleY(firstHalf, {
      fx: "name",
      y: (d) => d.votes / (quota / 100),
      //rx: "100%",
      sort: { fx: "Name" },
      fill: "status",
      rx: "100%",
      channels: {
        Name: "name",
        Votes: "votes",
        Party: "party",
        status: {
          value: "status",
          label: "Status in count"
        }
      },
      tip: {
        format: {
          Name: true,
          surname: false,
          status: false,
          Votes: true,
          Party: true,
          y: false,
          fx: false,
          stroke: false
        }
      }
    }),
    Plot.text(firstHalf, {
      fx: "name",
      text: (d) =>
        Math.floor((d.votes / quota) * 100).toLocaleString("en-US") + "%",
      frameAnchor: "bottom",
      lineAnchor: "top",
      dy: 8,
      fill: "status",
      fontSize: 20,
      fontFamily: "IBM Plex Sans",
      fontWeight: "bold"
    })
  ]
})
```

```js
waffleVotes1 = Plot.plot({
  style: { fontFamily: "IBM Plex Sans", fontSize: 14 },
  color: {
    legend: false,
    swatchWidth: 25,
    label: "Count status",
    domain: ["Elected", "Continuing", "Excluded"],
    order: ["Elected", "Continuing", "Excluded"],
    range: ["#ff7f0e", "#1f77b4", "#dadbdc"]
  },
  axis: null,
  width: 1000,
  fill: "status",
  label: null,
  height: 300,
  marginTop: 20,
  marginBottom: 100,
  marks: [
    Plot.axisFx({
      lineWidth: 1,
      anchor: "bottom",
      //tickRotate: 0,
      dy: 30,
      fontWeight: "bold"
    }),
    Plot.waffleY(
      { length: 1 },
      {
        y: 100,
        fillOpacity: 0.4,
        rx: "100%"
      }
    ),
    Plot.waffleY(secondHalf, {
      fx: "name",
      sort: { fx: "Name" },
      y: (d) => d.votes / (quota / 100),
      rx: "100%",
      fill: "status",
      tip: true,
      channels: {
        Name: "name",
        Votes: "votes",
        Party: "party",
        status: {
          value: "status",
          label: "Status in count"
        }
      },
      tip: {
        format: {
          Name: true,
          status: false,
          Votes: true,
          Party: true,
          y: false,
          fx: false,
          stroke: false
        }
      }
    }),
    Plot.text(secondHalf, {
      fx: "name",
      lineWidth: 1,
      text: (d) =>
        Math.floor((d.votes / quota) * 100).toLocaleString("en-US") + "%",
      frameAnchor: "bottom",
      lineAnchor: "top",
      dy: 8,
      fill: "status",
      fontSize: 20,
      fontFamily: "IBM Plex Sans",
      fontWeight: "bold"
    })
  ]
})
```

```js
viewof colorChoice = Inputs.select(choices, {
  label: "Colour chart by",
  format: (x) => x.name,
  value: choices.find((t) => t.name === "Candidate status"),
  width: 800
})
```

```js
lollipopChart = Plot.plot({
  width: 790,
  marginBottom: 45,
  marginLeft: 220,
  marginRight: 70,
  color: {
    legend: true,
    swatchWidth: 25,
    marginLeft: 10,
    columns: 3,
    domain: colorChoice.domain,
    order: colorChoice.order,
    range: colorChoice.range
  },
  style: { fontSize: 14, fontFamily: "IBM Plex Sans", padding: "5px" },
  subtitle: htl.html`These were the vote standings after <strong>count No. ${
    finalCount.map((d) => d.count)[0]
  }</strong> in <strong>${selectConstituency}</strong>. The quota was <strong>${format(
    quota
  )}</strong>.`,
  x: {
    tickPadding: 6,
    tickSize: 5,
    grid: true,
    label: "Votes",
    nice: true
    //tickRotate: -15
  },
  y: { label: "Candidate", nice: true, tickSize: 0 },
  marks: [
    Plot.ruleY(
      finalCount,
      //.filter((d) => d.votes > 1)
      {
        x: "votes",
        y: "name",
        strokeWidth: 2,
        stroke: colorChoice,
        sort: { y: "-x" }
      }
    ),
    Plot.text(
      finalCount.filter((d) => d.votes > 1),
      {
        x: "votes",
        y: "name",
        text: (d) => d.votes,
        dy: 0,
        dx: 30
      }
    ),
    Plot.dot(
      finalCount,
      //.filter((d) => d.votes > 1)
      {
        x: "votes",
        y: "name",
        fill: colorChoice,
        r: 4,
        //filter: (d) => d.votes > 1,
        sort: { y: "-x" },
        title: (d) =>
          `After count No. ${d.count}, ${d.name}, ${d.party}, had ${format(
            d.votes
          )} votes, with the quota standing at ${format(quota)} votes`
      }
    )
  ]
})
```

```js
viewof selectTable = Inputs.select(tableChoice, {
  label: "Candidates by",
  width: 790,
  format: (x) => x.name,
  value: tableChoice.find((t) => t.name === "Name, alphabetically")
})
```

```js
table_1 = htl.html`<head>
<style>
table {
  border-collapse: collapse;
  width: 100%;
  padding: 5px;
}

th, td {
  padding: 4px;
  text-align: left;
  border-bottom: 1px solid #DDD;
}

tr:hover {background-color: #e3dbdb;}
</style>
</head>
<body>
<table>
  <thead>
    <tr>
      <th><strong>Name</strong></th>
      <th><strong>Party</strong></th>
        <th><strong>Transfer</strong></th>
            <th><strong>Votes</strong></th>
           
      <th><strong>Status</strong></th>
      
    </tr>
  </thead>
  <tbody>
    ${selectTable.value.map(
      (selectTable) => htl.html`<tr>
       <td>${selectTable["name"]}</td>
      <td>${selectTable["party"]}</td>
       <td>${format(selectTable["transfers"])}</td>
      <td>${format(selectTable["votes"])}</td>
      <td><strong>${selectTable["status"]}</strong></td>
    </tr>`
    )}
  </tbody>
  </table>
  <body/>`
```

```js
byCandidate = md`## Explore by candidate
This election had **${
  Array.from(names).length
} candidates**. Explore their electoral path with the dropdown list.`
```

```js
viewof candidate = Inputs.select(
  Array.from(new Set(resultsByConstituency.map((d) => d.name))),
  { label: "Candidate", width: width }
)
```

```js
candidatePlot = Plot.plot({
  marginLeft: 70,
  marginBottom: 35,
  style: { fontSize: 14 },
  width: 790,
  color: {
    legend: true,
    swatchWidth: 25,
    columns: 3,
    domain: ["Elected", "Continuing", "Eliminated"],
    order: ["Elected", "Continuing", "Eliminated"],
    range: ["#ff7f0e", "#1f77b4", "#dadbdc"]
  },
  subtitle: htl.html`Results by count for <strong>${candidateRecord[0].name}</strong>, who ran in the election as a <strong>${candidateRecord[0].party}</strong> candidate<br><br>`,
  x: { label: "Count No.", labelAnchor: "center" },
  y: { grid: true, label: "↑ Votes" },
  marks: [
    () => htl.svg`<defs>
      <linearGradient id="gradient" gradientTransform="rotate(90)">
        <stop offset="100%" stop-color="#7F6C2E" stop-opacity="0.95" />
        <stop offset="90%" stop-color="#666666" stop-opacity="0.5" />
      </linearGradient>
    </defs>`,
    Plot.barY(candidateRecord, {
      x: "count",
      y: "votes",
      fill: "status",
      title: (d) =>
        `After count No. ${d.count}, ${d.name} had ${format(
          d.votes
        )} votes, with the quota standing at ${format(quota)} votes, and was ${
          d.status[0].toLowerCase() + d.status.slice(1)
        } in the count.`
    }),
    Plot.tickY(candidateRecord, {
      x: "count",
      y: "votes",
      fill: "party"
    }),
    Plot.ruleY([0])
  ]
})
```

```js
table_2 = htl.html`<head>
<style>
table {
  border-collapse: collapse;
  width: 100%,
}

th, td {
  padding: 4px;
  text-align: left;
  border-bottom: 1px solid #DDD;
}

tr:hover {background-color: #e3dbdb;}
</style>
</head>
<body>
<table>
  <thead>
    <tr>
      <th><strong>After count No.</strong></th>
 <th><strong>Transfer</strong></th>
            <th><strong>Votes</strong></th>
<th><strong>Status</strong></th>
      
    </tr>
  </thead>
  <tbody>
    ${candidateRecord.slice(0, 40).map(
      (candidateRecord) => htl.html`<tr>
      <td>${candidateRecord["count"]}</td>
<td>${format(candidateRecord["transfers"])}</td>
      <td>${format(candidateRecord["votes"])}</td>
 <td><strong>${candidateRecord["status"]}</strong></td>
    </tr>`
    )}
  </tbody>
  </table>
  <body/>`
```

```js
downloadButton = button(csvDownload, `election results dataset.csv`)
```

```js echo
partiesDomain = partiesPalette.map((d) => d.name)
```

```js echo
partiesPalette = [
  { name: "Fianna Fáil", value: "#2c8737" },
  { name: "Sinn Féin", value: "#088460" },
  { name: "Fine Gael", value: "#303591" },
  { name: "Independent", value: "#666666" },
  { name: "Labour Party", value: "#c82832" },
  { name: "Social Democrats", value: "#782b81" },
  { name: "Independent Ireland", value: "#087b87" },
  { name: "People Before Profit-Solidarity", value: "#be417d" },
  { name: "Aontú", value: "#b35400" },
  { name: "100% RDR", value: "#985564" },
  { name: "Green Party", value: "#6c7e26" }
]
```

```js echo
partiesRange = partiesPalette.map((d) => d.value)
```

```js echo
fallbackColors = [
  "#1f77b4",
  "#ff7f0e",
  "#2ca02c",
  "#d62728",
  "#9467bd",
  "#8c564b",
  "#e377c2",
  "#7f7f7f",
  "#bcbd22",
  "#17becf"
]
```

```js echo
completePartyDomain = [
  ...new Set([
    ...partiesDomain,
    ...fallbackColors.map((_, i) => `Other ${i + 1}`)
  ])
]
```

```js echo
completePartyRange = [...partiesRange, ...fallbackColors]
```

```js
resultsByConstituency = results.filter(
  (d) => d.constituency === selectConstituency
)
```

```table echo

```

```js echo
countByName = d3.group(resultsByConstituency, (d) => d.name)
```

```js echo
candidateRecord = countByName.get(`${candidate}`)
```

```js
sortedNames = names
  .sort((a, b) => d3.ascending(a, b))
  .map((full) => {
    const [last, first] = full.split(", ");
    return { name: `${first} ${last}` };
  })
```

```js
constituencies = Array.from(new Set(results.map((d) => d.constituency))).sort(
  (a, b) => d3.ascending(a, b)
)
```

```js
finalCountByPositionForTable = finalCount.slice().sort((a, b) => {
  const aInvalid = a.votes === null || isNaN(a.votes);
  const bInvalid = b.votes === null || isNaN(b.votes);

  if (aInvalid && !bInvalid) return 1; // Push `null` and `NaN` values to the end
  if (bInvalid && !aInvalid) return -1;

  return d3.descending(a.votes, b.votes);
})
```

```js
finalCountBySurname = finalCount
  .slice()
  .sort((a, b) => d3.ascending(a.name, b.name))
```

```js
tableChoice = [
  {
    name: "Status of poll",
    value: finalCountByPositionForTable
  },
  { name: "Name, alphabetically", value: finalCountBySurname }
]
```

```js
choices = {
  const partiesPalette = [
    { name: "Fianna Fáil", value: "#40b34e" },
    { name: "Sinn Féin", value: "#088460" },
    { name: "Fine Gael", value: "#303591" },
    { name: "Independent", value: "#666666" },
    { name: "Labour Party", value: "#c82832" },
    { name: "Social Democrats", value: "#782b81" },
    { name: "Independent Ireland", value: "#17becf" },
    { name: "People Before Profit-Solidarity", value: "#c5568b" },
    { name: "Aontú", value: "#ff7f0e" },
    { name: "100% RDR", value: "#985564" },
    { name: "Green Party", value: "#b4d143" }
  ];

  const fallbackColors = [
    "#1f77b4",
    "#ff7f0e",
    "#2ca02c",
    "#d62728",
    "#9467bd",
    "#8c564b",
    "#e377c2",
    "#7f7f7f",
    "#bcbd22",
    "#17becf"
  ];

  // Create full map of party → color
  const fullMap = new Map(partiesPalette.map((d) => [d.name, d.value]));
  let fallbackIndex = 0;

  // Collect unique parties in current view (e.g., constituency)
  const partiesInUse = Array.from(new Set(finalCount.map((d) => d.party)));

  // Extend map with fallbacks as needed
  for (const party of partiesInUse) {
    if (!fullMap.has(party)) {
      fullMap.set(party, fallbackColors[fallbackIndex % fallbackColors.length]);
      fallbackIndex++;
    }
  }

  // Build filtered domain/range only for used parties
  const usedDomain = partiesInUse;
  const usedRange = partiesInUse.map((p) => fullMap.get(p));

  return [
    {
      name: "Party",
      value: "party",
      domain: usedDomain,
      range: usedRange
    },
    {
      name: "Candidate status",
      value: "status",
      domain: ["Elected", "Continuing", "Eliminated"],
      order: ["Elected", "Continuing", "Eliminated"],
      range: ["#ff7f0e", "#1f77b4", "#dadbdc"]
    }
  ];
}
```

```js
finalCount = resultsByConstituency
  .filter((d) => d.count === countNo)
  .flatMap((d) => ({
    name: d.name,
    votes: d.votes,
    count: d.count,
    party: d.party,
    transfers: d.transfers || 0,
    status: d.status || "Continuing"
  }))
```

```js echo
finishingCount = d3.max(resultsByConstituency, (d) => d.count)
```

```js
resultCount = resultsByConstituency
  .filter((d) => d.count === finishingCount)
  .flatMap((d) => ({
    name: d.name,
    votes: d.votes,
    count: d.count,
    party: d.party,
    transfers: d.allocation || 0,
    status: d.status || "Continuing"
  }))
```

```js
results = election_20241.flatMap((d) => ({
  constituency: d["constituency_x"],
  name: d["candidate"],
  party: d.party,
  status: d.condition,
  count: d.count,
  votes: d.total,
  transfers: d.transfer,
  quota: d.quota,
  seats: d.seats,
  gender: d.gender === "M" ? "Male" : d.gender === "F" ? "Female" : d.gender,
  barRace: new Date(d["count"] * 88000000)
}))
```

```table echo

```

```js echo
results.filter((d) => d.constituency === "Carlow-Kilkenny")
```

```js
totalCount = d3.max(resultsByConstituency.map((d) => d.count))
```

```js echo
format = d3.format(",d")
```

```js echo
quota = d3.mode(Array.from(resultsByConstituency.map((d) => d.quota)))
```

```js echo
seats = Array.from(new Set(resultsByConstituency.map((d) => d.seats)))
```

```js echo
finalCountforWaffle = finalCount.slice() //.filter(d => d.votes > 0)
```

```js echo
half = Math.ceil(finalCountforWaffle.length / 2)
```

```js echo
firstHalf = finalCountforWaffle.slice(0, half)
```

```js echo
secondHalf = finalCountforWaffle.slice(half)
```

```js echo
finalCount
```

```js
prev = new Map(
  nameframes.flatMap(([, data]) => d3.pairs(data, (a, b) => [b, a]))
)
```

```js
next = new Map(nameframes.flatMap(([, data]) => d3.pairs(data)))
```

```js
function labels(svg) {
  let label = svg
    .append("g")
    .style("font-family", "bold 14px var(IBM Plex Sans)")
    .style("font-variant-numeric", "tabular-nums")
    .attr("text-anchor", "end")
    .selectAll("text");

  return ([date, data], transition) =>
    (label = label
      .data(data.slice(0, n), (d) => d.name)
      .join(
        (enter) =>
          enter
            .append("text")
            .attr(
              "transform",
              (d) =>
                `translate(${x((prev.get(d) || d).value)},${y(
                  (prev.get(d) || d).rank
                )})`
            )
            .attr("y", y.bandwidth() / 2)
            .attr("x", -6)
            .attr("dy", "-0.25em")
            .attr("fill", "white")
            .text((d) => d.name)
            .call((text) =>
              text
                .append("tspan")
                .attr("fill-opacity", 1)
                .attr("font-family", "IBM Plex Sans")
                .attr("font-weight", "normal")
                .attr("x", -6)
                .attr("dy", "1.15em")
            ),
        (update) => update,
        (exit) =>
          exit
            .transition(transition)
            .remove()
            .attr(
              "transform",
              (d) =>
                `translate(${x((next.get(d) || d).value)},${y(
                  (next.get(d) || d).rank
                )})`
            )
            .call((g) =>
              g
                .select("tspan")
                .tween("text", (d) =>
                  textTween(d.value, (next.get(d) || d).value)
                )
            )
      )
      .call((bar) =>
        bar
          .transition(transition)
          .attr("transform", (d) => `translate(${x(d.value)},${y(d.rank)})`)
          .call((g) =>
            g
              .select("tspan")
              .tween("text", (d) =>
                textTween((prev.get(d) || d).value, d.value)
              )
          )
      ));
}
```

```js
height = marginTop + barSize * n + marginBottom
```

```js
import { marginLeft } from "1d97f9d402b6b57b"
```

```js
import { marginBottom } from "1d97f9d402b6b57b"
```

```js
import {marginRight} from "1d97f9d402b6b57b"
```

```js
import {marginTop} from "1d97f9d402b6b57b"
```

```js
import {barSize} from "1d97f9d402b6b57b"
```

```js
import {y} from "1d97f9d402b6b57b"
```

```js
import {x} from "1d97f9d402b6b57b"
```

```js
function bars(svg) {
  let bar = svg.append("g").attr("fill-opacity", 1).selectAll("rect");

  return ([date, data], transition) =>
    (bar = bar
      .data(data.slice(0, n), (d) => d.name)
      .join(
        (enter) =>
          enter
            .append("rect")
            .attr("fill", color)
            .attr("height", y.bandwidth())
            .attr("x", x(0))
            .attr("y", (d) => y((prev.get(d) || d).rank))
            .attr("width", (d) => x((prev.get(d) || d).value) - x(0)),
        (update) => update,
        (exit) =>
          exit
            .transition(transition)
            .remove()
            .attr("y", (d) => y((next.get(d) || d).rank))
            .attr("width", (d) => x((next.get(d) || d).value) - x(0))
      )
      .call((bar) =>
        bar
          .transition(transition)
          .attr("y", (d) => y(d.rank))
          .attr("width", (d) => x(d.value) - x(0))
      ));
}
```

```js
function ticker(svg) {
  const now = svg.append("text")
      .style("font", `bold ${barSize}px var(--sans-serif)`)
      .style("font-variant-numeric", "IBM Plex Sans")
      .attr("fill", "444444")   
      .attr("text-anchor", "end")
      .attr("x", width - 6)
      .attr("y", marginTop + barSize * (n - 0.45))
      .attr("dy", "0.32em")
      .text(formatDate(keyframes[0][0]));

  return ([date], transition) => {
    transition.end().then(() => now.text(`Count ${formatDate(date)}`));
  };
}
```

```js
import {formatDate} from "1d97f9d402b6b57b"
```

```js
function axis(svg) {
  const g = svg.append("g").attr("transform", `translate(0,${marginTop})`);

  const axis = d3
    .axisTop(x)
    .ticks(width / 160, tickFormat)
    .tickSizeOuter(0)
    .tickSizeInner(-barSize * (n + y.padding()));

  return (_, transition) => {
    g.transition(transition).call(axis);
    g.select(".tick:first-of-type text").remove();
    g.selectAll(".tick:not(:first-of-type) line")
      .attr("stroke", "#666666")
      .attr("stroke-opacity", 0.5);
    g.select(".domain").remove();
  };
}
```

```js
import {tickFormat} from "1d97f9d402b6b57b"
```

```js
import {textTween} from "1d97f9d402b6b57b"
```

```js
color = {
  const scale = d3.scaleOrdinal(d3.schemeCategory10);
  if (resultsByConstituency.some((d) => d.category !== undefined)) {
    const categoryByName = new Map(
      resultsByConstituency.map((d) => [d.name, d.category])
    );
    scale.domain(categoryByName.values());
    return (d) => scale(categoryByName.get(d.name));
  }
  return (d) => scale(d.name);
}
```

```js
nameframes = d3.groups(
  keyframes.flatMap(([, data]) => data),
  (d) => d.name
)
```

```js
formatNumber = d3.format(",d")
```

```js
keyframes = {
  const keyframes = [];
  let ka, a, kb, b;
  for ([[ka, a], [kb, b]] of d3.pairs(datevalues)) {
    for (let i = 0; i < k; ++i) {
      const t = i / k;
      keyframes.push([
        new Date(ka * (1 - t) + kb * t),
        rank((name) => (a.get(name) || 0) * (1 - t) + (b.get(name) || 0) * t)
      ]);
    }
  }
  keyframes.push([new Date(kb), rank((name) => b.get(name) || 0)]);

  return keyframes;
}
```

```js
k = 12
```

```js
duration = 150
```

```js
n = Array.from(names).length
```

```js
function rank(value) {
  const data = Array.from(names, (name) => ({ name, value: value(name) }));
  data.sort((a, b) => d3.descending(a.value, b.value));
  for (let i = 0; i < data.length; ++i) data[i].rank = Math.min(n, i);
  return data;
}
```

```js
names = Array.from(new Set(resultsByConstituency.map((d) => d.name)))
```

```js
datevalues = Array.from(
  d3.rollup(
    resultsByConstituency,
    ([d]) => d.votes,
    (d) => +d.barRace,
    (d) => d.name
  )
)
  .map(([date, data]) => [new Date(date), data])
  .sort(([a], [b]) => d3.ascending(a, b))
```

```js
csvDownload = results.map((d) => ({
  constituency: d.constituency,
  name: d.name,
  status: d.status,
  count: d.count,
  votes: d.votes,
  party: d.party,
  transfers: d.transfers,
  quota: d.quota,
  seats: d.seats,
  gender: d.gender
}))
```

```js
button = (data, filename = "data.csv") => {
  if (!data) throw new Error("Array of data required as first argument");

  let downloadData;
  if (filename.includes(".csv")) {
    downloadData = new Blob([d3.csvFormat(data)], { type: "text/csv" });
  } else {
    downloadData = new Blob([JSON.stringify(data, null, 2)], {
      type: "application/json"
    });
  }

  const size = (downloadData.size / 1024).toFixed(0);
  const button = DOM.download(
    downloadData,
    filename,
    `Download ${filename} (~${size} KB)`
  );
  return button;
}
```

```js
lastCount = finalCount.map((d) => d.count).slice(0, 1)
```

```js
elected = lastCountArray.filter((d) => d.status == "Elected")
```

```js echo
finalElected = resultCount.filter((d) => d.status == "Elected")
```

```js
electedList = elected.length > 0
  ? elected
      .map((d) => d.name)
      .slice()
      .map((d) => d + ", ")
  : "No candidate has been deemed elected yet" //+ (elected.slice(-1).map(d => d.name))
//[]
```

```js
finalElectedList = finalElected.length > 0
  ? finalElected
      .map((d) => d.name)
      .slice()
      .map((d) => d + ", ")
  : "No candidate has been deemed elected yet" //+ (elected.slice(-1).map(d => d.name))
//[]
```

```js echo
eliminated = lastCountArray.filter((d) => d.status == "Eliminated")
```

```js echo
finalEliminated = resultCount.filter((d) => d.status == "Eliminated")
```

```js echo
eliminatedList = eliminated.length > 0
  ? eliminated
      .map((d) => d.name)
      .slice(0, -1)
      .map((d) => d + ", ")
  : "No candidate has been excluded yet" //+ (elected.slice(-1).map(d => d))
```

```js echo
finalEliminatedList = finalEliminated.length > 0
  ? finalEliminated
      .map((d) => d.name)
      .slice()
      .map((d) => d + ", ")
  : "No candidate has been excluded yet" //+ (elected.slice(-1).map(d => d))
```

```js echo
import { constituenciesFromAPI as constituencyFile } from "b472a3aa909bb0e2"
```

```js echo
tds = TDs.filter((d) => d.Constituency === `${selectConstituency}`).map(
  (d) => ({
    name: d.Deputy,
    constituency: d.Constituency,
    party: d.Party,
    page: "https://www.oireachtas.ie/en/members/member/" + d.Code.slice(),
    URI: d.Code.slice(),
    representCode: d.ConCode
  })
)
```

```js echo
import { data as TDs } from "e6d4a1e76a251e8e"
```

```js echo
filtered = constituencyFile.features.filter(
  (d) => d.properties.ENG_NAME_VALUE.slice(0, -4) == `${selectConstituency}`
)
```

```js echo
generateTDsSentence = (tds) => {
  const selectedTDs = tds.slice(0, Math.max(3, tds.length));
  return `The TDs elected were ${selectedTDs
    .map((td) => `<a href="${td.page}" target="_blank">${td.name}</a>`)
    .join(", ")}.`;
}
```

```js echo
lastCountArray = resultsByConstituency
  .filter((d) => d.count === lastCount[0])
  .flatMap((d) => ({
    name: d.name,
    votes: d.votes,
    count: d.count,
    party: d.party,
    transfers: d.allocation || 0,
    status: d.status || "Continuing"
  }))
```
